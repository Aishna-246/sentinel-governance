[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sentinel-governance"
version = "0.1.0"
description = "AI Governance Platform"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
include = ["sentinel*", "api*"]
